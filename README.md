# Interpretability of Small Model Selection Model
Epic dude 😎.

## Setup
You need to create a config file called `configs/model_selection.yaml` using `configs/TEMPLATE.yaml` to run `main.ipynb`.